package org.example;

/**
 * Escreva a diferença entre interfaces, instancias, objetos e classes no contexto de orientação a objeto:
 */

/**
 * Interfaces: Ela abrange apenas métodos formando meio que uma espécie de contrato quando usada, sendo aquela classe que a implementar tendo que usar todos os métodos dessa interface.
 *
 * Classes: É uma abstração de algo, podendo ter atributos como características e métodos representando as ações desta abstração.
 *
 * Instancia: Para inicializar um objeto, você precisará instânciá-lo primeiro. É no processo de instanciação que acontece a contrução do objeto.
 *
 * Objeto: Após instanciado, o objeto se torna a aplicação da classe, tendo os seus atributos definidos com valores e e seus métodos podendo ser acessado para fazer qualquer ação dentro do código.
 */
public class Main {
    public static void main(String[] args) {
    }
}